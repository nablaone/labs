# labs

This repo is a versioned lab — a monorepo of unrelated, self-contained
experiments (Go, AWS, Garmin Connect IQ, OpenSCAD models, etc.), each in its
own top-level directory. There's no shared build system or overarching
architecture; treat each directory as its own small project.
